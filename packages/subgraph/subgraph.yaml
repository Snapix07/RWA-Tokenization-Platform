specVersion: 0.0.5
description: RWA Tokenization Platform — indexes asset onboarding, vault positions, AMM swaps, and DAO governance.
repository: https://github.com/Snapix07/RWA-Tokenization-Platform

schema:
  file: ./schema.graphql

dataSources:
  # ── Asset Factory ────────────────────────────────────────────────────────────
  - kind: ethereum
    name: AssetFactory
    network: arbitrum-sepolia
    source:
      address: "0x0d41539C9B43cd675dEBBC3dB8754e26ec274eDF"
      abi: AssetFactory
      startBlock: 268346764
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - AssetToken
      abis:
        - name: AssetFactory
          file: ./abis/AssetFactory.json
      eventHandlers:
        - event: TokenDeployed(indexed bytes32,indexed address,bool)
          handler: handleTokenDeployed
      file: ./src/mappings.ts

  # ── RWA Vault ────────────────────────────────────────────────────────────────
  - kind: ethereum
    name: RWAVault
    network: arbitrum-sepolia
    source:
      address: "0xFbECEB9447925e0c2e433f5eD674F7110AF67018"
      abi: RWAVault
      startBlock: 268346800
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - VaultPosition
      abis:
        - name: RWAVault
          file: ./abis/RWAVault.json
      eventHandlers:
        - event: Deposit(indexed address,indexed address,uint256,uint256)
          handler: handleDeposit
        - event: Withdraw(indexed address,indexed address,indexed address,uint256,uint256)
          handler: handleWithdraw
      file: ./src/mappings.ts

  # ── AMM ──────────────────────────────────────────────────────────────────────
  - kind: ethereum
    name: RWAAMM
    network: arbitrum-sepolia
    source:
      address: "0x658eD17F2686A652ACC89162c7aA99b957e7938E"
      abi: RWAAMM
      startBlock: 268346812
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - AmmSwap
      abis:
        - name: RWAAMM
          file: ./abis/RWAAMM.json
      eventHandlers:
        - event: Swap(indexed address,indexed address,uint256,uint256)
          handler: handleSwap
        - event: Sync(uint112,uint112)
          handler: handleSync
      file: ./src/mappings.ts

  # ── Governor ─────────────────────────────────────────────────────────────────
  - kind: ethereum
    name: RWAGovernor
    network: arbitrum-sepolia
    source:
      address: "0x091858adb6f82c323c4B4d1b0aA59Cec953B9E75"
      abi: RWAGovernor
      startBlock: 268346836
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - GovernanceProposal
        - ProposalVote
      abis:
        - name: RWAGovernor
          file: ./abis/RWAGovernor.json
      eventHandlers:
        - event: ProposalCreated(uint256,address,address[],uint256[],string[],bytes[],uint256,uint256,string)
          handler: handleProposalCreated
        - event: VoteCast(indexed address,uint256,uint8,uint256,string)
          handler: handleVoteCast
        - event: ProposalQueued(uint256,uint256)
          handler: handleProposalQueued
        - event: ProposalExecuted(uint256)
          handler: handleProposalExecuted
        - event: ProposalCanceled(uint256)
          handler: handleProposalCanceled
      file: ./src/mappings.ts
